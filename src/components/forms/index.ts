export const styles: Record<string, React.CSSProperties> = {
    FormWrapper: {
        display: "flex",
        flexDirection: "row",
        gap: "2rem",
        flexWrap: "wrap",
        justifyContent: "center"
    }
}
